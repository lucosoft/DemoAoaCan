The CAN related Android Open Accessory Demo consists of 

- One AOA Board that acts as a gateway.

- One or more "CAN nodes".

- An Android device with the Demo_AOA_Nodes App installed.
  The device connects to the gateway and controls the nodes.

This sample application is an implementation of the 
gateway that runs on the AOA Board (EA-APP-001).

